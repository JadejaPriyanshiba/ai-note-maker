# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# YOUTUBE_API_KEY: Required for the Shorts Learning feature's YouTube video search.
# Create one at https://console.cloud.google.com/apis/credentials with the
# "YouTube Data API v3" enabled. Server-side only, never exposed to the client.
YOUTUBE_API_KEY="MY_YOUTUBE_API_KEY"

# like i like coding, i want to automate things, learn AI concepts and agents, automate things using free tools and make them myself... but i am open to know free unique efficient alternative along side too. i am a student just trying to explore things, for the concepts and steps we will need to go from simplest to complex from the basics to expert levels